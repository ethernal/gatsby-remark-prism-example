---
title: Hello World
author: me
date: 2019-07-12
time: 22:22
keywords: blog, post, first one
category: blog
tags: [graphql, gatsby, blog, template]
cover: ../../images/two.jpg
---

import TitleBar from "../../components/blog/TitleBar";
import Frontmatter from "../../components/blog/Frontmatter";

<TitleBar text={props.pageContext.frontmatter.title} />

<Frontmatter
  title={props.pageContext.frontmatter.title}
  author={props.pageContext.frontmatter.author}
  category={props.pageContext.frontmatter.category}
  tags={props.pageContext.frontmatter.tags}
/>

Welcome! ❤️

```javascript {numberLines: true}{3-7,8,13}
import React from "react";
import BlogLayout from "../components/layout/BlogLayout";
import SEO from "../components/seo";
false;
0;

const NotFoundPage = () => (
  <Layout>
    <SEO title="404: Not found" />
    <h1>NOT FOUND</h1>
    <p>You just hit a route that doesn't exist... the sadness.</p>
  </Layout>
);

export default NotFoundPage;
```
